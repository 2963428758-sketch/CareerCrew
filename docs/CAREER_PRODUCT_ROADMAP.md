# CareerCrew 产品完善与新增功能路线图

更新时间：2026-09-06（第一/二/三期已交付；第四期已启动，P0/P1 完成见下）。

## 目标与实施约定

围绕一个目标岗位贯通 JD、岗位专属简历、模拟面试与后续跟进。用户已确认：先完整交付第一期，再按计划推进第二、三期。第四期起重心从「增加独立功能」转向「让所有数据真正围绕一个岗位形成完整档案」。第一期至第三期执行细节见 [实施计划](superpowers/plans/2026-09-06-job-preparation-phase1.md)，浏览器验收见 [验收报告](superpowers/plans/2026-09-06-job-preparation-verification.md)。

当前基础：React / FastAPI 对话、岗位检索、简历上传解析、逐题面试、长期记忆、质检与评测。已有能力延伸为产品流程，不重复建设 Agent。

## 第四期：岗位档案与实际求职转化（进行中）

核心目标：让一个岗位拥有自己的完整档案——简历版本、投递材料、行动任务、联系人/HR 跟进、面试会话与复盘、Offer 与最终结果，并守住「AI 准备、人工确认、完整留痕」的边界（明确不做全自动批量投递）。

### 已完成（2026-09-06）

| 优先级 | 项 | 结果 |
|---|---|---|
| P0 | 前端测试全绿基线 | ✅ 修复 4 个既有失败（groupTurns 规格回归 ×2、KnowledgePanel/KnowledgePage 过时断言 ×2）；vitest 197/197 |
| P0 | 测试凭据与账号清理 | ✅ 验收报告移除明文密码；qa_tester/qa_other 及全部测试数据已从开发库删除 |
| P0 | 真实岗位检索验收 | ✅ Chrome CDP 实测：Boss直聘实时抓取深圳 8 个真实 Java 岗位、结构化卡片随历史持久化；发现并修复「无 JD 岗位收藏必然 422」缺口（禁用收藏+引导手动录入） |
| P1 | 统一岗位档案绑定 | ✅ 行动任务 / HR 跟进 / Offer 创建时可选关联岗位（列表带岗位徽标）；素材库按设计保持跨岗位共享 |
| P1 | 岗位详情时间线 | ✅ `GET /api/career/opportunities/{id}/timeline` 聚合收藏→版本→会话→阶段→任务→HR→Offer→复盘；岗位详情页时间线面板 |
| P1 | 提醒中心与 ICS 日历 | ✅ `GET /api/career/reminders` 聚合任务到期/逾期、看板跟进、HR 待办（行动计划 tab 顶部面板）；`GET /api/career/reminders/ics` 导出 ICS 导入系统日历（推送通知依赖部署通道，暂以日历订阅替代） |
| P1 | 通用岗位采集器 | ✅ 书签脚本（岗位准备页生成/复制）：任意招聘网站选中 JD → 一键跳回本页预填录入表单（公司/JD/来源链接），纯前端跳转无跨域问题 |
| P2 | ATS 简历体检 | ✅ `POST /api/career/opportunities/{id}/ats-check`：联系方式/章节/时间线/量化成果/篇幅/JD 覆盖率等确定性规则，明确标注「规则检查 · 非 AI」；求职工具箱面板 |
| P2 | 岗位专属投递材料包 | ✅ `POST /api/career/opportunities/{id}/application-kit`：求职信/自我介绍/招呼语/跟进话术/感谢信，LLM 语义生成 + 模板兜底；一键复制、可存为素材（标记未确认草稿，发送前始终人工确认） |
| P2 | 联系人与内推管理 | ✅ 迁移 0006 `job_contacts` + CRUD API + 求职中心「联系人」tab（角色/渠道/联系方式/下次联系时间/关联岗位） |
| P2 | 求职效果归因 | ✅ `GET /api/career/stats` 增 `by_source`：按岗位来源细分阶段分布与样本量；效果统计 tab 展示 |
| P2 | 语音表达辅助 | ✅ 面试作答后即时反馈：语速（字/分）、口头禅计数、STAR 完整度启发式与单场平均语速（标注「启发式提示，非评分」；录音级分析留待后续） |
| P2 | 联系人提醒联动 | ✅ 联系人「下次联系时间」纳入提醒中心（逾期/7 天内）与 ICS 日历导出 |
| P2 | 简历版本效果归因 | ✅ 岗位详情可标记「投递所用版本」；统计按版本细分已投递/面试/Offer（显式样本量）；未标记不计入 |
| P3 | 公司与面试情报包 | ✅ `POST /api/career/opportunities/{id}/intel-brief`：公司推断、可能问题、待向面试官确认的问题、可用证据；LLM 生成 + 模板兜底，外部事实统一标注「未经核实，请自行验证」 |
| P3 | 导师协作与只读分享 | ✅ 迁移 0007 令牌表；整包/单版本两种范围、7-30 天有效期、一键撤销、可选联系方式脱敏；公开页 `/share/:token` 无需登录（失效一律 404） |

### 待办（按优先级）

| 优先级 | 项 | 说明 |
|---|---|---|
| P3 | 录音级语音分析 | 基于录音的语速/停顿/口头禅识别（当前为文本启发式） |
| P3 | 分享内容扩展 | 时间线/复盘报告纳入分享范围；分享访问审计 |
| — | 第四期全部 P0/P1/P2 及 P3 核心项已交付 | 剩余均为增强型条目，按真实使用反馈排期 |


## 第一期：完成一次岗位准备（✅ 已交付）

1. **岗位卡片**：从成功检索的结构化结果渲染公司、岗位、城市、薪资、来源、JD；卡片随对话持久化，旧记录没有结构化数据时仍显示原回答。
2. **手动录入与收藏**：独立“岗位准备”入口；填写公司、岗位、JD、来源链接；必填校验、编辑、搜索、删除；刷新不丢失、账号隔离。第一期手动入口采用粘贴文本，截图解析继续使用已有附件能力，自动从链接抓取不在本期范围。
3. **岗位关联**：每个收藏保存 JD 快照；同一岗位下关联简历版本和准备会话，来源链接仅允许 HTTP/HTTPS。收藏不会自动投递或联系 HR。
4. **简历版本与编辑**：从现有简历库选取原文，也可粘贴原文；手动编辑、原文/当前稿对照、保存不可变版本、查看历史版本。简历顾问的完整回答可由用户显式载入编辑器，确认后保存，避免将解释文字直接当正式简历。
5. **导出**：指定已保存版本导出真正的 PDF 和 DOCX；中文可读、换页不丢字、名称安全。保留原始上传简历，不覆盖。
6. **针对岗位的模拟面试**：从岗位及选定版本创建准备会话，进入现有面试模块；清楚展示使用的岗位和简历版本；首次输入预填，用户点击开始后才调用 AI；后续轮次及历史会话继续使用关联上下文。

### 第一期验收场景

- 用户手动录入“测试公司 / Java 开发 / JD”，刷新后仍能找到；另一个账号不能读取、编辑、导出或关联这条数据。
- 用户在匹配结果收藏岗位，不需要重新粘贴 JD；重新打开有结构化结果的历史对话仍能看到卡片。
- 在该岗位下选择已有简历、编辑并保存两个版本，旧版本保持不变；分别下载 PDF/DOCX，与选定版本一致。
- 从指定版本进入简历优化或面试，自动带入同一份 JD 与简历；切到其他会话不带错上下文；刷新后历史关联能恢复。
- 无 JD、空简历、超长输入、危险链接、重复点击、接口失败均给出可理解反馈；保存失败保留编辑内容。

## 第二期：持续求职（✅ 已交付）

| 功能 | 状态 | 实现位置 |
|---|---|---|
| 求职进度看板 | ✅ | `/career` 看板 tab：六阶段、下一步/日期/备注、变更记录、归档 |
| 项目经历素材库 | ✅ | `/career` 素材库 tab：背景/职责/行动/成果/标签/确认 |
| 整场面试复盘 | ✅ | `POST /api/career/interview-review` + 面试复盘 tab（规则汇总 + AI 增强） |
| 行动计划与提醒 | ✅ | `/career` 行动计划 tab：创建/完成/延期/关闭提醒、过期提示 |
| HR 跟进中心 | ✅ | `/career` HR 跟进 tab：录入/待办/回复草稿（用户确认，绝不自动发送） |
| 首次使用引导 | ✅ | `OnboardingDialog`（阶段/城市/目标，可跳过，`career_profiles` 持久化） |
| 事实确认与记忆纠正 | ✅（最小交付） | 素材 confirmed 标记 + 画像；记忆深度集成沿用现有记忆管理 |
| 岗位准备包 | ✅（简化） | 岗位详情页集中呈现 JD 快照 + 版本 + 导出 + 可解释匹配 |
| 任务失败恢复 | ✅ | 匹配页错误卡「重试上一问」；输入持久化可编辑重发 |

## 第三期：个性化与效果复盘（✅ 已交付，个别项为最小交付）

| 功能 | 状态 | 实现位置 |
|---|---|---|
| 可解释岗位匹配 | ✅ | `POST /api/career/opportunities/{id}/gap-analysis` + 岗位详情面板（要求→证据→缺口/资料未提及） |
| 技能差距与学习任务 | ✅（并入可解释匹配） | 缺口条目可直接查看；转练习任务沿用行动计划 |
| Offer 对比 | ✅ | `/career` Offer 对比 tab：权重可调、缺值显式「待确认」 |
| 真实面试复盘录入 | ✅ | `/career` 面试复盘 tab：录入/薄弱点提炼，与模拟结果区分 |
| 语音模拟面试 | ✅（最小交付） | 面试页语音作答（Web Speech API，转写可校正），追问/限时留待后续 |
| 求职效果统计 | ✅ | `/career` 效果统计 tab：阶段分布 + 转化率（分子/分母显式，小样本不下结论） |
| 全局搜索与归档 | ✅ | `GET /api/career/search` + 看板归档（archived_at） |
| 数据与隐私控制 | ✅ | `/career` 数据与隐私 tab：导出 JSON、删除全部求职数据（含结果说明） |

## 持续工程工作

- 业务关联采用明确 ID 与 owner 约束，不能依赖 LLM 回忆替代数据库。
- 已有质检与评测增加跨模块、资料矛盾、用户纠正和工具失败场景。
- 延迟/Token 按任务分析，优化重复调用并设置预算；不因提速放宽事实依据。
- 岗位来源、采集时间、失效状态和历史 JD 快照逐步统一。

## 验证与交付边界

第一期先通过隔离测试和页面交互验证；数据库结构使用 Alembic 新增迁移。不得对开发库执行清库式测试，不自动投递、发送消息、提交 Git 或推送。部署与线上数据库迁移另行记录实际执行结果。
