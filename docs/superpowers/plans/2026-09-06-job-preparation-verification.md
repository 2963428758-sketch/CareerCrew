# 岗位准备一~三期 · 浏览器端到端验收报告

日期：2026-09-06　环境：本地开发（Windows）　分支：`codex/job-preparation-phase1`（未提交）

## 一、环境准备（本轮由助手代操作）

| 项目 | 结果 |
|---|---|
| Docker Desktop 启动 | ✅ 已启动，postgres:16 / qdrant 容器自动恢复（沿用 `pgdata` 卷，既有数据完好） |
| 数据库迁移 | ✅ `alembic upgrade head`：0002 → **0005_job_lifecycle (head)**，12 张新表全部创建 |
| 迁移修复 | ⚠️ **0003_user_settings 迁移存在既有 bug**（`bind.execute(裸字符串)` 在 SQLAlchemy 2.x 抛 `ObjectNotExecutableError`，导致该库此前一直卡在 0002）。已改为 `text()` 包装后迁移链打通。该修复影响的是既有迁移文件，不是本轮新增迁移。 |
| 测试账号 | ✅ 新建临时管理员 `qa_tester` 与隔离抽查账号 `qa_other`（明文密码不落文档；验收后均已删除） |
| 后端 | ✅ `uvicorn careercrew_api.main:app --port 8000`，`/` 200，登录接口 200，DASHSCOPE key 已配置（真实 LLM 可用） |
| 前端 | ✅ `npm run dev`（vite，端口 5176，代理 `/api` → 8000） |

## 二、浏览器端到端实测结果（ZCode 内置浏览器，1440×900）

### 第一期：打通一次岗位准备

| # | 场景 | 结果 | 证据 |
|---|---|---|---|
| 1 | 登录 qa_tester | ✅ | 进入主界面，侧边栏出现全部模块 |
| 2 | 首次使用引导自动弹出 | ✅ | 填写「在职看机会/深圳/大模型应用工程师」→ `career_profiles` 落库（`onboarding_done=t`） |
| 3 | /preparation 空表单保存被拦 | ✅ | 显示「公司名称不能为空」，无请求发出 |
| 4 | 手动录入岗位并保存 | ✅ | `preparation_opportunities` 新增：云帆科技/大模型应用工程师/深圳/28-45K/Boss直聘/URL |
| 5 | 详情面板完整渲染 | ✅ | JD 快照、简历版本工作台、可解释匹配面板、导出按钮 |
| 6 | 保存两个不可变版本 | ✅ | 「第一版·基础定制」「第二版·突出RAG量化」均在版本列表，旧版本未被覆盖 |
| 7 | 导出 PDF / DOCX | ✅ | 下载事件均触发；API 复核：PDF 1.7 有效、`filename*=UTF-8''第二版·突出RAG量化.pdf`、DOCX 为合法 OOXML（Word 2007+） |
| 8 | 开始模拟面试（确认弹窗 → 跳转） | ✅ | 确认弹窗显示岗位与版本；跳转 `/interview` 后侧边栏出现「云帆科技大模型应用工程师模拟面试」会话 |
| 9 | 准备会话横幅与预填 | ✅ | 横幅「岗位准备：云帆科技 · 大模型应用工程师 · 简历版本『第二版·突出RAG量化』已自动带入」+「查看」链接；输入框预填开场语，点击发送才调用 AI |
| 10 | **真实 LLM 上下文注入** | ✅ | 面试官首题明确引用注入的 JD 与简历量化数据（+18%、-40%、RRF、BGE-M3），证明 r-prep-/i-prep- 每轮注入生效 |
| 11 | 作答 → 评分 → 黄金范例 → 追问 | ✅ | 逐题评分、改进点（❌ 标注）、黄金回答范例、追问全部真实返回 |
| 12 | 会话数据落库 | ✅ | `conversations.legacy_thread_id=i-prep-…`，4 条消息（user/assistant × 2 轮）全部 completed |
| 13 | 刷新后关联恢复 | ✅ | 整页刷新后从侧边栏点选该会话，横幅与历史问答完整恢复 |
| 14 | 语音作答入口 | ✅ | 面试页出现「开始语音输入」按钮（Web Speech API 可用环境） |

### 第二期：持续求职（/career 求职中心）

| # | 场景 | 结果 | 证据 |
|---|---|---|---|
| 15 | 进度看板渲染 | ✅ | 六列布局，云帆科技在「待准备」列 |
| 16 | 阶段流转 + 变更记录 | ✅ | 点击「已投递」→ 列间移动；「记录」显示 `2026-09-06 · 待准备 → 已投递` |
| 17 | 行动计划 | ✅ | 创建「准备三道 RAG 面试题（截止 2026-09-08）」→ 勾选完成 |
| 18 | 项目素材库 | ✅ | 新建「企业知识库 RAG 问答系统」（背景/职责/行动/成果/标签），带「已确认」徽标 |
| 19 | HR 跟进 | ✅ | 录入 HR 沟通 + 待办（周三前确认）→ 存草稿 → 确认草稿（显示「已确认（发送由你完成）」，无自动发送） |
| 20 | Offer 对比 | ✅ | 录入 35K×16 + 期权等，加权参考分显示 |
| 21 | 效果统计 | ✅ | 已投递 1、转化率（分子/分母显式）、各阶段分布 |

### 第三期：个性化与复盘

| # | 场景 | 结果 | 证据 |
|---|---|---|---|
| 22 | 生成整场复盘报告（面试页按钮） | ✅ | `interview_reports` 落库：`source=llm`、2 题均分 3.75、真实总结 |
| 23 | 报告列表（面试复盘 tab） | ✅ | 「模拟面试复盘报告」区显示均分 3.75 / AI 汇总 / 薄弱点 |
| 24 | 一键复练薄弱项 | ✅ | 跳转 `/interview`，输入框预填「请针对以下薄弱点出题训练：…（AI 总结的断层点）」 |
| 25 | 真实面试复盘录入入口 | ✅ | 同 tab 表单存在（未重复提交，逻辑与素材表单同构且已有 API 测试覆盖） |
| 26 | 全局搜索 | ✅ | 「RAG」命中素材「企业知识库 RAG 问答系统」与统计计数行 |
| 27 | 数据导出/删除入口 | ✅ | 「删除全部求职数据」确认弹窗正常（已取消，未真删）；导出为 JSON 下载 |

### 安全与隔离（线上抽查）

- `qa_other` 访问 `qa_tester` 的岗位 → **404**；岗位列表/素材/复盘报告 → **[]**，无跨账号泄漏。

## 三、测试中发现并修复的问题

| 问题 | 级别 | 修复 |
|---|---|---|
| **面试页整页白屏**：`useSpeechRecognition` 用 `useState` 保存 SpeechRecognition 构造函数，React 把函数值当惰性初始化器调用 → `Please use the 'new' operator` → React 卸载整棵树 | **P0（功能不可用）** | 改为模块级常量解析构造器（`useSpeechRecognition.ts` 重写），实测恢复 |
| 模拟面试复盘报告有后端 API 但无 UI 入口，用户不可达 | P1 | 面试页头部新增「生成整场复盘报告」；面试复盘 tab 新增报告列表 + 一键复练（sessionStorage 一次性交接） |
| 迁移 0003 裸字符串执行在 SQLAlchemy 2.x 报错（既有 bug，卡住整条迁移链） | P1（部署阻塞） | `text()` 包装（migrations/versions/0003_user_settings.py） |
| 表单空提交校验、确认弹窗等 | — | 均按预期工作，无需修复 |

另记录：两次「白屏」假象分别为 Vite HMR 半更新脏状态与热更新瞬态（整页重载即恢复），非代码缺陷。

## 四、最终回归

- 后端定向回归（preparation/career/chat/auth）：**48 项全通过**
- 前端：`tsc --noEmit` 无错误；vitest **192 通过 / 4 失败**——4 个失败为改动前即存在的 Knowledge/turn 既有失败（已用干净工作树复现确认）
- `vite build` 成功

## 五、遗留与说明

1. 测试账号 `qa_tester`、`qa_other` 及其全部测试数据已在验收完成后删除，开发库不留测试凭据。
2. 浏览器实测中职位匹配页的实时岗位检索未跑（需 Chrome CDP 登录 Boss直聘/猎聘，属外部依赖）；岗位卡片链路已由 API 级测试（jobs 透传）+ 收藏交互测试覆盖。
3. 前后端当前仍在后台运行：前端 http://localhost:5176 ，后端 :8000。手动重启命令见 README「启动项目」。
4. 代码仍未提交，全部改动在工作区 `codex/job-preparation-phase1` 分支上。

---

# 第四期 · 浏览器端到端验收报告（2026-09-06 补充）

## 一、环境

- 前后端重启至最新代码（后端 :8000 / 前端 :5176），Chrome CDP 运行中
- 临时账号 `qa_ui`（密码随机不落文档，验收后连同数据删除）
- 数据库：迁移 0006/0007 已应用

## 二、实测结果（浏览器端到端）

### 岗位准备（第四期新增能力）

| # | 场景 | 结果 |
|---|---|---|
| 1 | 通用岗位采集器入口：侧边栏展示书签代码 + 复制按钮 | ✅ |
| 2 | 手动录入岗位（星河智能 · Agent 平台工程师） | ✅ 落库并进入详情 |
| 3 | 保存简历版本后求职工具箱版本下拉自动刷新 | ✅（修复后，见缺陷 D1） |
| 4 | ATS 体检：规则徽标、JD 覆盖率、联系方式/章节检查 | ✅ 全部渲染 |
| 5 | 投递材料包生成（LLM 未预热走模板兜底）：五段话术 + 保存为素材 | ✅ |
| 6 | 面试情报包：公司推断/可能问题/待确认问题 + 「未经核实」声明 | ✅ |
| 7 | 投递所用版本标记 → 数据库 `applied_version_id` 写入 | ✅ |
| 8 | 岗位时间线：收藏 + 版本事件按时间展示 | ✅ |
| 9 | 分享面板：整包分享（默认脱敏）生成 + 列表 + 复制链接 | ✅ |
| 10 | 数据库确认：分享令牌 active、mask_pii=true | ✅ |

### 求职中心

| # | 场景 | 结果 |
|---|---|---|
| 11 | 提醒面板渲染（行动计划 tab 顶部） | ✅ |
| 12 | ICS 日历导出（下载事件触发） | ✅ |
| 13 | 创建任务时可关联岗位，列表显示岗位徽标 | ✅ |
| 14 | 联系人 tab：创建 + 下次联系时间展示 | ✅（岗位徽标未显示系脚本未选岗位，数据为空，非缺陷） |
| 15 | 联系人「下次联系时间」进入提醒中心（逾期徽标 + 「联系 星河智能 李内推」） | ✅ |
| 16 | 效果统计：来源归因 + 简历版本归因（投递版·星河智能出现在版本区） | ✅ |

### 面试与公开分享

| # | 场景 | 结果 |
|---|---|---|
| 17 | 从岗位发起模拟面试：横幅 + 首轮预填 + 真实 LLM 出题引用注入的简历量化数据 | ✅ |
| 18 | 语音表达反馈面板：103 字 · 语速约 167 字/分 · 口头禅 4 处（然后/就是/那个/其实逐一命中）· STAR 已覆盖 T·A/R，提示补齐 S | ✅ |
| 19 | 登出后访问 /share/:token：无需登录、只读头、岗位/JD/版本内容、PII 已隐藏、截止时间与失效提示 | ✅（修复后，见缺陷 D3） |

### 检索基线复用

- 真实岗位检索（Boss直聘实时抓取深圳 8 个真实岗位）已在第三期验收时实测通过，本轮未重复执行；CDP 通道运行正常（:9222 探活 200）。

## 三、实测发现并修复的缺陷

| ID | 缺陷 | 级别 | 修复 |
|---|---|---|---|
| D1 | 保存简历版本后，求职工具箱/分享面板的版本下拉不刷新（按钮保持禁用） | P2 | ResumeWorkspace 保存成功后广播 `preparation:versions-changed` 事件，JobToolsPanel/SharePanel 监听并重载版本列表 |
| D2 | （上轮遗留路径）恢复在途流占位符无 turnId，新分组规则下会显示为孤儿 turn | P3 | 四个对话页占位符统一带上一条 assistant 的 turnId |
| D3 | **公开分享页登出后不可用**：① App 的 /share/ 旁路放在登录检查之后，未登录用户被登录页拦截；② SharePage 用 useParams 但渲染在路由上下文之外，token 恒为空 | **P0（功能不可用）** | 旁路移到所有登录态检查之前；SharePage 改为从地址栏直接解析令牌 |

## 四、最终回归

- 前端：tsc 无错误；**vitest 197/197（46 个文件全部通过）**；vite build 成功
- 后端：career/preparation/chat/auth 定向回归 60+ 项全通过
- 测试账号 `qa_ui` 及全部数据已删除，无残留测试凭据
